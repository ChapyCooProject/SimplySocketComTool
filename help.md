# このソフトについて（簡易ソケット通信ソフト）

---
## はじめに

- 本ソフトウェアは、TCP/IPソケット通信試験用の簡易ツールです。
- インストールは不要で、実行ファイル単体で動作します。
- 64bit版のWindowsに対応しています。32bit版のWindowsでは起動しません。

---
## 起動と終了

- 実行ファイル「簡易ソケット通信ソフト.exe」を起動してください。
- 終了する場合は、画面左上のメニュー「終了」をクリックしてください。

---
## 通信モードの選択と開始

### サーバモード
1. 「通信モード」で "TCPサーバ" を選択します。
2. 以下の情報を設定します。
-- ポート番号：待ち受けするポート番号
-- 文字コード：通信で使用する文字コード
3. 「開始」ボタンで Listen 状態を開始します。

### クライアントモード
1. 「通信モード」で "TCPクライアント" を選択します。
2. 以下の情報を設定します。
-- IPアドレス：接続先サーバのIPアドレス
-- ポート番号：接続先サーバのポート番号
-- タイムアウト：接続が確立されるまでの待機時間（秒）
-- 文字コード：通信で使用する文字コード
3. 「開始」ボタンで サーバへの接続を試みます。

---
## データ送信方法

### 送信データの入力
- 画面右上の「送信データ」入力欄に送信したい文字列を入力します。
- 1行ずつ送信する場合は改行で区切ります。
- 空行は送信しません。

### 手動送信
- 「手動送信」ボタンを押すと、次のルールにしたがって入力欄の文字列を送信します。
-- テキスト内の文字列を一度に送信：入力欄の文字列を全て一度に送信
-- テキスト内の文字列を1行ずつ送信：入力欄の文字列を先頭行から順番に送信（ボタン押下ごとに１行進む）
- 送信のタイミングとは関係なく、指定秒ごとに受信データを取得します。
-- ただし、制御コード「ACK, NAK, ENQ, EOT, CR, LF, CRLF」を受信した際は、即時取得します。

### 自動送信
- 「自動送信」ボタンを押すと、次のルールにしたがって入力欄の文字列を先頭行から順番に送信します。
-- 応答メッセージ（ACK/NAK）を利用して送受信：応答メッセージを受信したら自動的に次のデータを送信（3秒後タイムアウト）
-- 時間指定毎に送信：指定した時間が経過したら自動的に次のデータを送信

---
## 送信データにおける注意事項

- 画面右上の「送信データ」入力欄内の改行は送信されません。
  改行コードを送信したい時は "<CR><LF>" などを電文に記述してください。

- 制御コードを使用する際は、括弧付きの文字で表記してください。
  対応している制御コードは以下のとおりです。
  - 0x00: <NUL>
  - 0x01: <SOH>
  - 0x02: <STX>
  - 0x03: <ETX>
  - 0x04: <EOT>
  - 0x05: <ENQ>
  - 0x06: <ACK>
  - 0x07: <BEL>
  - 0x08: <BS>
  - 0x09: <HT>
  - 0x0A: <LF>
  - 0x0B: <VT>
  - 0x0C: <FF>
  - 0x0D: <CR>
  - 0x0E: <SO>
  - 0x0F: <SI>
  - 0x10: <DLE>
  - 0x11: <DC1>
  - 0x12: <DC2>
  - 0x13: <DC3>
  - 0x14: <DC4>
  - 0x15: <NAK>
  - 0x16: <SYN>
  - 0x17: <ETB>
  - 0x18: <CAN>
  - 0x19: <EM>
  - 0x1A: <SUB>
  - 0x1B: <ESC>
  - 0x1C: <FS>
  - 0x1D: <GS>
  - 0x1E: <RS>
  - 0x1F: <US>
  - 0x7F: <DEL>

---
## ダウンロード

- 実行ファイルは、下記URLからダウンロードできます。
  https://drive.google.com/drive/folders/1ahBVBlvV3750aWLEC8GdlKCSQtdHHPFC?usp=sharing
